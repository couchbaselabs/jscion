<div class="messages">
  <label>messages</label>
  <ul>
    {{#.messages:i}}
      <li intro="fade:slow">
        <button class="btn btn-mini btn-danger" on-click="deleteMessage" value="{{i}}">delete</button>
        {{.updatedAt}} - {{.message}}
      </li>
    {{/.messages}}
  </ul>
  <div>
    {{#doComment}}
      <textarea id="commentMessage" value="{{commentMessage}}" placeholder="your new comment here"></textarea>
    {{/doComment}}
    <div class="control">
      {{#doComment}}
        <button class="btn btn-small" on-click="saveComment">save comment</button>
      {{/doComment}}
      <button class="btn btn-small" on-click="addComment">{{ doComment ? 'cancel' : 'add' }} comment</button>
    </div>
  </div>
</div>
